# Активный план работы

## Текущая задача

Добавить файловые HTTP-логи с ротацией: видеть входящие запросы, ответы и ошибки; удалять логи старше 5 дней; исключить директорию логов из git.

## Чеклист

- [completed] Проверить front controller и Response, где можно надежно снять статус и тело ответа.
- [completed] Добавить HTTP logger с JSONL-файлами по дням и очисткой файлов старше 5 дней.
- [completed] Встроить логирование в `Application::handle()` вокруг обработки запроса и ошибок.
- [completed] Добавить директорию логов в `.gitignore` и документацию по настройке.
- [in_progress] Прогнать PHP lint и HTTP-проверку логов/ротации в Docker.
- [pending] Обновить `AI_CURRENT_CONTEXT.md`, проверить diff/status, сделать коммит и push.

## Заметки

- Рабочее дерево на старте чистое: `master...origin/master`.
- Логи должны быть локальными runtime-файлами и не попадать в git.
- Для безопасности request/response bodies логируем с ограничением размера, но без скрытия payload, чтобы было видно поведение эмулятора при разработке.
