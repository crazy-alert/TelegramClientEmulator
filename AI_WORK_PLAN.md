# Активный план работы

## Текущая задача

Нет активных задач. Очередь `.aitasks/` пуста.

## Ближайшие этапы (по ROADMAP.md)

1. **Этап 3**: Long Polling — `GET|POST /bot{token}/getUpdates`, поддержка offset/limit/timeout
2. **Этап 4**: Webhook-доставка — `setWebhook`, `deleteWebhook`, `getWebhookInfo`, отправка update на webhook URL
3. **Этап 5**: Ответы бота — `sendMessage`, сохранение bot messages, отображение в чате
